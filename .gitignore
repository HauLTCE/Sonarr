# Environment variables & secrets (NEVER commit — Discord token, SSH/deploy password, Lavalink pw)
.env
.env.*
*.env
!.env.example
# Private keys / credential files
*.pem
*.key
*.p12
*.pfx
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
credentials.json
secrets.json

# .NET
bin/
obj/
*.user
*.suo
artifacts/
.vs/
TestResults/
*.received.*

# Node / Next.js
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.pnpm-store/

# Python (legacy bot in _bot_legacy/)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
develop-eggs/
downloads/
eggs/
.eggs/
parts/
sdist/
wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Databases + their WAL/SHM sidecars (user data — never commit).
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.sqlite-wal
*.sqlite-shm
*.sqlite3-wal
*.sqlite3-shm

# JSON data files (user / guild data)
playlists.json
levels.json
ai_memory.json
server_config.json

# One-time migration leftovers (renamed copies of the JSON data above — same content)
*.migrated

# Runtime data (legacy bot's SQLite/vector store/per-user state) and scratch dirs
data/
team/

# System
.DS_Store
Thumbs.db

# Logs
*.log
# Server log dumps (contain user messages/IDs — never commit)
_server_log_raw.txt
